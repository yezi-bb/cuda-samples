# 第 1 周：工程骨架与构建

对应 README：Getting Started、Building CUDA Samples、Install Samples、Running All Samples as Tests、Dependencies。

本周不追求跑很多 kernel。结束后应能指出：样例怎么编、公共错误检查在哪、为什么有的样例会被 CMake 跳过、`run_tests.py` 怎么决定参数。

## 周一：按 README 画仓库地图

**读**

- [README.md](../README.md) 从头到「Samples list」
- 十个分类入口：`cpp/0_Introduction/README.md` 到 `cpp/9_CUDA_Tile/README.md`，以及 README 里的 Python 布局表
- 根目录 `CHANGELOG.md` 只看最近一节，知道这是 Toolkit 13.4 对应的样例树

**做**

- 画一张目录图：`cpp/` 十类、`python/` 四类、`Common/`、`cmake/`、`run_tests.py`、`test_args.json`
- 标出 README 明确说「顶层 CMake 不编译」的部分：整个 `python/`

**想清楚**

1. 样例按什么分册，为什么 0 到 9 不是难度线性递增，而是主题分册？
2. `bandwidthTest` 为什么从 Utilities 里删了，替代品是什么？
3. 贡献模型现在接不接受外部补丁？代码风格指向哪份规范？

**完成标准**：不看 README，能说出每一册的一句话职责。

## 周二：在 Windows 上编通一个样例

**读**

- README「Windows」和「Building a Single Sample」
- 根 `CMakeLists.txt`：`cmake_minimum_required(3.20)`、语言 `C CXX CUDA`、`CMAKE_CUDA_ARCHITECTURES` 的默认逻辑
- `cmake/CudaSampleArchs.cmake` 里的主架构列表，知道默认会编哪些 SM

**做**

- 确认本机 `nvcc`、`cmake`、Visual Studio 生成器名称
- 只构建 `cpp/1_Utilities/deviceQuery`（Utilities 最适合当探针）。架构先用 `native` 试一次；若生成器不接受 `native`，改成设备的计算能力数字（`deviceQuery` 输出里的 major.minor，例如 8.9 写成 `89`）
- 把成功的生成器名和 `<arch>` 写进笔记，后面 15 周都用这一对

**想清楚**

1. 为什么单独进样例目录配置时，必须自己传 `CMAKE_CUDA_ARCHITECTURES`？
2. 全量 `cmake ..` 不传架构时，时间会花在哪里？
3. `ENABLE_CUDA_DEBUG` 打开后，编译器少做了什么，为什么默认关闭？

**完成标准**：`deviceQuery` 的 Release 可执行文件能打印本机 GPU 名称和计算能力。

## 周三：公共代码，而不是样例本身

**读**

- `Common/helper_cuda.h`：`checkCudaErrors`、`getLastCudaError`、`findCudaDevice`、`gpuGetMaxGflopsDeviceId`、`_ConvertSMVer2Cores`
- `Common/helper_cuda_drvapi.h`、`Common/helper_string.h`、`Common/helper_timer.h` 的头注释和主要入口
- 任意一个已能打开的样例 `CMakeLists.txt`，看它怎样把 `Common` 加进 include 路径

**做**

- 在 `deviceQuery` 源文件里搜 `findCudaDevice` 或 `checkCudaErrors`，确认样例没有自己再写一套错误处理
- 列出后面会反复见到的 6 个头文件：`helper_cuda.h`、`helper_string.h`、`helper_timer.h`、`helper_math.h`、`helper_image.h`、`helper_multiprocess.h`

**想清楚**

1. `checkCudaErrors` 失败时进程怎么退出，和 kernel 启动错误为什么常常要再调 `getLastCudaError`？
2. `EXIT_WAIVED`（值为 2）和普通失败有什么区别？这和后面的测试脚本有什么关系？
3. 选设备的默认策略是「第 0 块」还是「理论峰值更高的那块」？

**完成标准**：能指出一个样例从命令行 `-device=` 到真正 `cudaSetDevice` 的函数链。

## 周四：安装布局和批量测试

**读**

- README「Install Samples」和「Running All Samples as Tests」
- `run_tests.py` 前 120 行：怎么找可执行文件、怎么匹配 `test_args.json`
- `test_args.json` 里三种形态各找一个例子：`skip`、单次 `args`、多次 `runs`（README 用 `fluidsGL`、`ptxgen`、`recursiveGaussian` 举例）

**做**

- 不要在本周跑完全部测试。只对周二编出的 `deviceQuery` 做一次：`python run_tests.py --dir <该可执行文件所在目录> --config test_args.json`
- 记下安装路径模板：`build/bin/${TARGET_ARCH}/${TARGET_OS}/${BUILD_TYPE}`，Windows 例子是 `build/bin/amd64/windows/release`

**想清楚**

1. README 为什么强调这些样例不是 CUDA 的验证套件？
2. 图形样例为什么经常标 `skip`？
3. 找不到 JSON 条目时，脚本会传什么参数？

**完成标准**：能解释一次测试运行的退出码含义（0 成功、非 0 失败、样例自己 waive）。

## 周五：依赖矩阵，决定后面哪些天改走读

**读**

- README「Dependencies」整节：第三方库和 CUDA 特性两张清单
- 根目录和 `cpp/` 下 CMake 里对缺依赖「waive / 跳过子目录」的方式（抽查 `cpp/5_Domain_Specific/CMakeLists.txt`）

**做**

- 做一张本机能力表，每项只写「有 / 没有」：
  - 独显数量、是否同一型号（影响 P2P）
  - OpenGL、Vulkan SDK、DirectX 12、FreeImage、MPI、OpenMP
  - 计算能力是否 ≥ 6.0（多块协作组）、≥ 7.0（Tensor Core 样例下限）、≥ 8.0（L2 访问策略、异步拷贝）
- 把第 8、12、16 周里可能走读的样例先圈出来

**想清楚**

1. 缺 FreeImage 时，样例是编译失败还是配置阶段放弃？
2. 统一内存、动态并行、流优先级各自的架构或系统限制是什么？
3. Tegra 样例为什么要 `-DBUILD_TEGRA=True`，默认构建为什么不包含它们？

**完成标准**：本机能力表写完。周六用三句话总结：构建命令、公共错误检查、哪些册在本机只能走读。
